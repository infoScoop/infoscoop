infoScoop OpenSource 2.0.0 RC2 relesase note
============================================

How to update to RC2 from an old version.
-----------------------------------------
1. Re deploy infoscoop.war to WebApplication Server according to Quick Start.

2. Replace gadget files in the repository database.

@(1). Open SQL executable tool.
  (2). Execute following SQL command.
     > delete from IS_GADGETS where type in ('calc','blogparts','todoList','alarm','sticky','worldclock')
     > delete from IS_GADGET_ICONS where type in ('calc','blogparts','todoList','alarm','sticky','worldclock')
@(3). Open command pronpt, change directory to tools
  (4). Copy the suitable JDBC Driver to lib dir.
  (5). Execute following ccd ommand:
     >import.sh(bat) GADGET,GADGETICON

[Warning]Executing steps above, following gadgets settings is initialized:

* calc
* todoList
* alarm
* blogparts
* sticky
* worldclock

Follow issues are fixed on 2.0.0.RC2 release.
---------------------------------------------

Issue 22: When undefined variable is used for IS_AUTH_UID_PARAM_NAME:  "undefined" string  is transmitted.
Issue 23: An error occurs in gadget that uses pubsub feature. 
Issue 24: The reply doesn't return when "Content-Length Header" is transmitted by "makeRequest". 
Issue 25: An error occurs if the gadget whose content type is URL reads the gadget api. 
Issue 26: An error occurs if the gadget whose content type is URL does rpc. 
Issue 27: A value set in "User settings(initial value)" at gadget administration cannot be deleted.
Issue 28: Fail to read the RSS of @IT site
Issue 30: Gadget whose type attr is "url" and view atrr is  "Canvas" is not correctly displayed. 
Issue 31: There is no way to enable weblogic10.xml.
Issue 32: After the password is changed, must move to the login page but  incorrectly move to logined main page. 
Issue 33: There is no way to disable authenticationService.
Issue 34: All of the edit fields for modulePrefs are text-field.
Issue 35: When a rss item has no description,  the page linked the rss Item should be displayed in maximize mode. 
Issue 36: When "Gadget" is moved to "Tab", RPC puts out the error. 
Issue 37: Add a function to set "RPC Relay URL" from Gadget"
Issue 38: When failing in "getSubject" in SessionManagerFilter, "Admininistration Menus" is not displayed by "Administration Screen". 
Issue 39: When a log level is set as debug, a ajax request with NoOperation filter is failed. 
Issue 40: URL properties in initial SiteAggregationMenu data contain tabs and line feeds. 
Issue 41: InitializeServlet#initSessionCreateConfig method is not required.
Issue 43: A datetime format in Message console is  hour in am/pm.
Issue 44: A list datatype forms displayed on left border in Edit Panel.
Issue 45: Adjust height function must be disabled in Gadget that have required prefs in static panel.
Issue 48: Even if there is not five number of messages in a menu alert, "Show more messages" button is displayed.
Issue 49: HTTP basic authentication function is added to gadgets.io.makeRequest. 
Issue 52: Both refresh icon and indicator icon of the top menu are displayed only the first time.
Issue 53: When UserPref type is list, even if it has admin_datatype attribute, automatically changing the corresponding checkbox in "Select update properties" does not work.
Issue 54: When clicking "A" tag having href which value is "#" and onclick attributes in a gadget, 403 error occur.
Issue 55: In Chrome and Safari a scroll bar is displayed at any widget header.
Issue 56: Modify the code to show 403 HTTPstatus message and show 401 message instead in RSS reader.
Issue 57: In Safari 4,  a pulldown box in schedule gadgets expand too much.
Issue 59: The error occurs because it cannot distinguish when it is HTML when "Content-Type" header of the response is a capital letter when a fragment mini-browser is set. 
